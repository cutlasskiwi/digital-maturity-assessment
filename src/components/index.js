// Import and export all components for easier imports elsewhere
import Layout from './layout/Layout';

// Export individual components
export { Layout };

// Export default as a collection
export default {
  Layout
};